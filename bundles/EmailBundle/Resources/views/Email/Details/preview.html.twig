<div class="pr-md pl-md pb-lg mb-32 tile">
  <div class="type-fluid-heading-03 mt-md">{{ email.subject }}</div>
  <div class="row">
    <hr class="mt-sm" />
  </div>
  <div class="d-flex gap-sm ai-start">
    <div
      class="d-inline-flex as-1-1 ai-center jc-center"
      style="background-color: #ffd0bc; border-radius: 100px; width: 50px;"
    >
      <span class="type-heading-03 fw-b">
        {{ email.fromName|default(configGetParameter('mailer_from_name'))|slice(0, 1)|upper }}
      </span>
    </div>
    <div>
      <div class="d-flex ai-center gap-xs mb-2 mt-4">
        <span class="type-label-02 fw-sb">
          {{ email.fromName|default(configGetParameter('mailer_from_name')) }}
        </span>
        <span class="type-label-01">&lt;{{ email.fromAddress|default(configGetParameter('mailer_from_email')) }}&gt;</span>
      </div>
      <span class="type-label-01 text-secondary">{{ 'mautic.email.preview.to_label'|trans }}</span>
      <span class="type-label-02">{{ 'mautic.email.preview.to_me'|trans }}</span>
      <div class="d-flex text-secondary gap-md mt-xs">
        <div class="d-flex ai-center">
          <i class="ri-calendar-line"></i>
          <span class="ml-xs type-label-01">{{ dateToDate('now') }}</span>
        </div>
        <div class="d-flex ai-center">
          <i class="ri-time-line"></i>
          <span class="ml-xs type-label-01">{{ dateToTime('now')|upper }}</span>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <hr class="mb-sm" />
  </div>
  <div id="email-details-header" class="d-flex gap-md">
    {% if email.replyToAddress %}
      <div class="d-flex gap-3 ai-center">
        <span class="type-label-01 text-secondary">{{ 'mautic.email.preview.reply_to'|trans }}</span>
        {% include '@MauticCore/Helper/_tag.html.twig' with {
          tags: [
            {
              color: 'warm-gray',
              label: email.replyToAddress|default(configGetParameter('mailer_from_email')),
              icon: 'ri-account-circle-fill',
            }
          ]
        } %}
      </div>
    {% endif %}
    {% if email.bccAddress %}
      <div class="d-flex gap-3 ai-center">
        <span class="type-label-01 text-secondary">{{ 'mautic.email.preview.bcc_label'|trans }}</span>
        {% include '@MauticCore/Helper/_tag.html.twig' with {
          tags: [
            {
              color: 'warm-gray',
              label: email.bccAddress|default(configGetParameter('mailer_from_email')),
              icon: 'ri-account-circle-fill',
            }
          ]
        } %}
      </div>
    {% else %}
      <div class="d-flex gap-3 ai-center ml-a">
        <span class="type-label-01 text-placeholder">{{ 'mautic.email.preview.bcc_placeholder'|trans }}</span>
      </div>
    {% endif %}
  </div>
  <div class="row">
    <hr class="mt-sm mb-0" />
  </div>
  <div class="row">
    <iframe
      id="content_preview_frame"
      scrolling="no"
      src="{{ previewUrl|e }}"
      title="{{ 'mautic.email.preview.iframe_title'|trans }}"
      style="width: 100%; min-height: 500px; border: 0; max-height: 500px; overflow: hidden;"
    ></iframe>
  </div>
</div>